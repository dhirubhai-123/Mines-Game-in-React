import { createContext } from "react";

export const minesCount = createContext();
export const playAgain = createContext();
export const tilesClickIndexes = createContext();
export const randomNumContext = createContext();
export const userLostContext = createContext();
export const scoreContext = createContext();